import 'package:flutter/material.dart';

class AppColors {
  // 🔵 Primary (Trust / System)
  static const Color primaryBlue = Color(0xFF0D47A1);
  static const Color primaryBlueLight = Color(0xFF1976D2);
  static const Color primaryBlueDark = Color(0xFF002171);

  // 🟠 Accent (Action / Emergency)
  static const Color accentOrange = Color(0xFFFF6F00);
  static const Color accentOrangeLight = Color(0xFFFFA000);
  static const Color accentOrangeDark = Color(0xFFE65100);

  // 🚨 Status Colors
  static const Color urgent = Color(0xFFD84315); // deep orange-red
  static const Color normal = Color(0xFF2E7D32); // calm green

  // 🎨 Backgrounds
  static const Color background = Color(0xFFF4F6F9);
  static const Color card = Colors.white;

  // ✍️ Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF616161);
  static const Color textLight = Colors.white;

  // 🔘 Buttons
  static const Color buttonPrimary = primaryBlue;
  static const Color buttonAccent = accentOrange;
  static const Color buttonDisabled = Color(0xFFBDBDBD);

  // 🟦 Borders & Divider
  static const Color border = Color(0xFFE0E0E0);
}
